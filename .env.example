# 環境変数サンプル
# このファイルをコピーして .env.local を作成し、実際の値を設定してください。
# .env.local は .gitignore によりコミットされません。

# Vercel KV（Upstash Redis）— チェックイン・訪問ログの永続化に使用
# Vercel ダッシュボード → Storage → KV → Connect で取得
KV_REST_API_URL=
KV_REST_API_TOKEN=
